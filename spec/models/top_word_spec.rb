require 'spec_helper'

describe TopWord do
	
	context "#self.raw_data" do
		it "should scrape the raw data for gettysburg address" do
			pending
		end
	end

	context "#self.four_score" do
		it "should iterate over raw data and store all unique words in db" do
			pending
		end
	end
end